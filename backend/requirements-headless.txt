# Опциональная зависимость beta-парсеров через браузер (MEGASTROY_HEADLESS=1,
# LEMAN_LIVE=1 — plans/2026-06-30-beta-headless-parser.md,
# plans/2026-07-10-leman-browser-fetch.md).
# Не входит в requirements.txt, чтобы не тащить Chromium в обязательный
# прод-образ. patchright — drop-in замена playwright с патчами против
# CDP-детекта (нужна для обхода Qrator у Лемана); используем её и для
# Мегастроя, чтобы не тащить оба пакета разом (конфликт версии pyee).
# Установка:
#   pip install -r requirements-headless.txt
#   patchright install chrome
patchright==1.61.2
