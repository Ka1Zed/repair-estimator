# Адрес backend для cross-origin деплоев.
# Если переменная не задана, фронт использует относительные пути (/api/*, /health),
# которые маршрутизирует Vite-прокси (npm run dev) или nginx (production).
# Для локальной разработки создайте frontend/.env.local:
#   VITE_API_URL=http://localhost:8000
